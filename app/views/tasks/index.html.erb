<h1>My tasks</h1>
<ul>
<% @tasks.each do |task| %>
<li> 
<%= link_to(task.title, task_path(task)) %> <%= link_to("Delete", delete_path(task), method: "delete") %> 
</li>
<% end %>
</ul>
<br>
<%= link_to("New task", new_task_path) %>